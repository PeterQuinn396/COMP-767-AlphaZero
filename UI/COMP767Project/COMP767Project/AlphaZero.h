#pragma once

//#include <torch/torch.h>
//#include <torch/nn.h>
//#include <torch/jit.h>

class AlphaZero //: public torch::nn::Module
{
public:
	AlphaZero(int, int, int);

    /*torch::nn::Linear input_layer;
    torch::nn::ModuleList fc_layers;
    torch::nn::Linear policy_layer;
    torch::nn::Linear value_layer;*/
};

